threshold = 2500; %HU for needle
pointsThreshold = 100; %number of points before connected component is considered important
roiRadius = 60;
fSize=4;

ptNum=10;
baselineScanNum=5003;
numCompare=2;
generateDiffOutputFiles( ptNum,baselineScanNum,numCompare,threshold,pointsThreshold,roiRadius,fSize);

ptNum=11;
baselineScanNum=5007;
numCompare=4;
generateDiffOutputFiles( ptNum,baselineScanNum,numCompare,threshold,pointsThreshold,roiRadius,fSize);

ptNum=2;
baselineScanNum=5008;
numCompare=2;
generateDiffOutputFiles( ptNum,baselineScanNum,numCompare,threshold,pointsThreshold,roiRadius,fSize);

ptNum=3;
baselineScanNum=5007;
numCompare=1;
generateDiffOutputFiles( ptNum,baselineScanNum,numCompare,threshold,pointsThreshold,roiRadius,fSize);

ptNum=3;
baselineScanNum=5011;
numCompare=1;
generateDiffOutputFiles( ptNum,baselineScanNum,numCompare,threshold,pointsThreshold,roiRadius,fSize);

ptNum=5;
baselineScanNum=5019;
numCompare=1;
generateDiffOutputFiles( ptNum,baselineScanNum,numCompare,threshold,pointsThreshold,roiRadius,fSize);

ptNum=5;
baselineScanNum=5024;
numCompare=1;
generateDiffOutputFiles( ptNum,baselineScanNum,numCompare,threshold,pointsThreshold,roiRadius,fSize);

ptNum=7;
baselineScanNum=5002;
numCompare=1;
generateDiffOutputFiles( ptNum,baselineScanNum,numCompare,threshold,pointsThreshold,roiRadius,fSize);

ptNum=7;
baselineScanNum=5004;
numCompare=1;
generateDiffOutputFiles( ptNum,baselineScanNum,numCompare,threshold,pointsThreshold,roiRadius,fSize);

ptNum=8;
baselineScanNum=5005;
numCompare=1;
generateDiffOutputFiles( ptNum,baselineScanNum,numCompare,threshold,pointsThreshold,roiRadius,fSize);

ptNum=9;
baselineScanNum=5008;
numCompare=1;
generateDiffOutputFiles( ptNum,baselineScanNum,numCompare,threshold,pointsThreshold,roiRadius,fSize);

ptNum=9;
baselineScanNum=5010;
numCompare=1;
generateDiffOutputFiles( ptNum,baselineScanNum,numCompare,threshold,pointsThreshold,roiRadius,fSize);

ptNum=12;
baselineScanNum=5002;
numCompare=1;
generateDiffOutputFiles( ptNum,baselineScanNum,numCompare,threshold,pointsThreshold,roiRadius,fSize);

ptNum=13;
baselineScanNum=6;
numCompare=2;
generateDiffOutputFiles( ptNum,baselineScanNum,numCompare,threshold,pointsThreshold,roiRadius,fSize);







